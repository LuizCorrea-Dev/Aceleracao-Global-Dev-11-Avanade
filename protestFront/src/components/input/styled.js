import styled from 'styled-components'

export const Input = styled.input`
  padding: 5px 10px;
  margin: 10px;

  &:focus {
    border-color: transparent;
    outline: 1px solid rgb(230,81,0);
  }
`
